name: WordPress Version Checker

on:
    schedule:
        - cron: '0 0 * * *'
    workflow_dispatch:

permissions:
    contents: write
    pull-requests: write

jobs:
    wp-version-checker:
        uses: linchpin/actions/.github/workflows/wp-version-checker.yml@v4
        secrets: inherit
